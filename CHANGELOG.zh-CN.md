# 更新日志

## [Unreleased]

### 新增

- Finder 右键菜单增加单个 ZIP 解压服务，复用菜单栏入口的保存位置、密码、安全校验和原子解压流程

## [1.2.3] - 2026-08-20

### 优化

- 统一菜单栏与 Finder 右键的加密 ZIP 设置弹窗、保存位置选择和创建规则

## [1.2.2] - 2026-08-20

### 优化

- 更新提示改为面向用户概括新版本的优化与修复，不再展示下载源和技术校验细节

## [1.2.1] - 2026-08-20

### 修复

- 解压时忽略 `__MACOSX/`、`.DS_Store` 和 AppleDouble `._*` 等 macOS 辅助元数据，不再把这些项目写入解压结果
- 解压完成弹窗复用加密压缩完成弹窗的紧凑标题栏，使应用图标与“ZIP 已解压”在同一行且尺寸一致，并移除输出目录与文件名之间的额外间距

## [1.2.0] - 2026-08-20

### 新增

- 解压弹窗增加“解压到独立文件夹”选项；默认仍创建 ZIP 同名独立目录，取消勾选时直接解压到 ZIP 所在目录，并在同一弹窗中完成输出位置选择和密码输入

### 安全与兼容性

- 直接解压先在目标目录内暂存并完成密码、认证和路径校验，顶层同名项目冲突时拒绝覆盖并回滚，不向原目录留下半成品

### 界面改进

- 统一加密压缩、解压、自动密码与完成弹窗的应用图标和紧凑布局；长路径从中间省略并保留文件名尾部，补齐菜单栏解压图标

## [1.1.0] - 2026-08-19

### 新增

- 增加按需 ZIP64 读取与创建；小归档继续使用普通 ZIP，并覆盖普通及 WinZip AES-256 ZIP64 互操作
- 创建与解压改为固定大小分块处理，使用临时文件或目录暂存，并在全部成功后原子提交
- 增加可取消的创建、解压和更新进度面板，以及菜单栏保存面板确认后的安全原子替换

### 安全与兼容性

- 收紧条目声明大小、总输出、条目数量、磁盘空间、路径碰撞和符号链接防护，认证通过前不写出加密明文
- 解压结果使用保守的 `0600`/`0700` 权限且不恢复可执行位；用户创建的 ZIP 遵循系统默认 `umask`
- 明确拒绝多卷 ZIP、ZipCrypto 和符号链接条目，并保留 Finder 右键入口不覆盖已有文件的行为

### 工程改进

- 拆分密码与文件名输入控件，统一密码校验和进度面板实现，移除重复与无生产消费方的代码
- CI 安装 7-Zip 运行双向及 ZIP64 互操作测试，并增加 Release 高熵流式基准、内存和时间回归门槛
- 发布脚本移除维护者机器绝对路径，改为显式校验中央规范依赖

## [1.0.0] - 2026-08-18

### 新增

- 完成首个原生 AppKit 菜单栏应用，支持创建 WinZip AES-256 加密 ZIP 和解压兼容 ZIP
- 接入 Finder 服务，以不覆盖现有文件的方式创建加密压缩包
- 增加认证后解压、路径穿越与符号链接逃逸防护、条目与输出大小限制及外部兼容夹具
- 增加每日自动检查和菜单手动检查更新，固定 GitHub 优先、Gitee 回退
- 更新替换前验证 Schema v2 更新清单、不可变发布溯源、SHA-256、Developer ID、Hardened Runtime、公证、Gatekeeper、应用身份和 arm64 架构
- 增加精确标签构建、签名、公证、发布溯源、中央源码发布、双镜像发布和显式安装命令
